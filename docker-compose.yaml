services:
  resume-builder:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ai_resume_builder
    ports:
      - "3000:3000"
    # DELETE the 'environment' section. 
    # The env_file below handles it automatically.
    env_file:
      - .env.local
    restart: unless-stopped